---
layout: people
title: People
---



<!-- List of graduate students -->
<h2 class="category-title">Group Leader</h2>
<div class="container">
	{% for category in site.data.group_members %} <!-- Displays students with 4 students to a row, adding new rows if needed-->
		{% if category.name == "Group Leader" %}
			{% assign count = 0 %} <!-- Initialize variable that counts to 4 -->
			{% for person in category.people %}
				{% assign count = count | plus: 1 %} <!-- Increment -->
				{% if forloop.first %} <!-- If this is the first item, start a row -->
					<div class="row row-centered">
				{% endif %}
					<div class="col-md-3 col-centered">
						<div class="thumbnail">
							<a href="http://physics.illinois.edu/people/profile.asp?vimartin">
								<img src="{{ person.image }}" class="img-circle">
							</a>
							<div class="caption"><h3>{{ person.name }}</h3>
								<h5 class="email">{{ person.email }}</h5>
							</div>				
						</div>
					</div>
				{% if count >= 4 and forloop.last == false %} <!-- If we've reached a multiple of 4, end this row and start a new one, unless this is the last item -->
					</div>
					<div class="row row-centered">
						{% assign count = 0 %} <!-- Rest the count -->
				{% elsif forloop.last %}
					</div> <!-- End the row if this is the last item -->
				{% endif %}
			{% endfor %}
		{% endif %}
	{% endfor %}
</div>

<center><img src="../img/paragraph-separator-2.gif" width=10%></center>

<!-- List of post-doctorates students -->
<h2 class="category-title">Post-Doctorates</h2>
<div class="container">
	{% for category in site.data.group_members %} <!-- Displays students with 4 students to a row, adding new rows if needed-->
		{% if category.name == "Postdocs" %}
			{% assign count = 0 %} <!-- Initialize variable that counts to 4 -->
			{% for person in category.people %}
				{% assign count = count | plus: 1 %} <!-- Increment -->
				{% if forloop.first %} <!-- If this is the first item, start a row -->
					<div class="row row-centered">
				{% endif %}
					<div class="col-md-3 col-centered">
						<div class="thumbnail">
							<a href="../people/{{ person.netid }}">
								<img src="{{ person.image }}" class="img-circle">
							</a>
							<div class="caption"><h3>{{ person.name }}</h3>
								<h5 class="email">{{ person.email }}</h5>
							</div>				
						</div>
					</div>
				{% if count >= 4 and forloop.last == false %} <!-- If we've reached a multiple of 4, end this row and start a new one, unless this is the last item -->
					</div>
					<div class="row row-centered">
						{% assign count = 0 %} <!-- Rest the count -->
				{% elsif forloop.last %}
					</div> <!-- End the row if this is the last item -->
				{% endif %}
			{% endfor %}
		{% endif %}
	{% endfor %}
</div>

<center><img src="../img/paragraph-separator-2.gif" width=10%></center>

<!-- List of graduate students -->
<h2 class="category-title">Graduate Students</h2>
<div class="container">
	{% for category in site.data.group_members %} <!-- Displays students with 4 students to a row, adding new rows if needed-->
		{% if category.name == "Graduate Students" %}
			{% assign count = 0 %} <!-- Initialize variable that counts to 4 -->
			{% for person in category.people %}
				{% assign count = count | plus: 1 %} <!-- Increment -->
				{% if forloop.first %} <!-- If this is the first item, start a row -->
					<div class="row row-centered">
				{% endif %}
					<div class="col-md-3 col-centered">
						<div class="thumbnail">
							<a href="../people/{{ person.netid }}">
								<img src="{{ person.image }}" class="img-circle">
							</a>
							<div class="caption"><h3>{{ person.name }}</h3>
								<h5 class="email">{{ person.email }}</h5>
							</div>				
						</div>
					</div>
				{% if count >= 4 and forloop.last == false %} <!-- If we've reached a multiple of 4, end this row and start a new one, unless this is the last item -->
					</div>
					<div class="row row-centered">
						{% assign count = 0 %} <!-- Rest the count -->
				{% elsif forloop.last %}
					</div> <!-- End the row if this is the last item -->
				{% endif %}
			{% endfor %}
		{% endif %}
	{% endfor %}
</div>

<center><img src="../img/paragraph-separator-2.gif" width=10%></center>

<!-- List of undergraduate students -->
<h2 class="category-title">Undergraduate Students</h2>
<div class="container">
	{% for category in site.data.group_members %} <!-- Displays students with 4 students to a row, adding new rows if needed-->
		{% if category.name == "Undergraduate Students" %}
			{% assign count = 0 %} <!-- Initialize variable that counts to 4 -->
			{% for person in category.people %}
				{% assign count = count | plus: 1 %} <!-- Increment -->
				{% if forloop.first %} <!-- If this is the first item, start a row -->
					<div class="row row-centered">
				{% endif %}
					<div class="col-md-3 col-centered">
						<div class="thumbnail">
							<a href="../people/{{ person.netid }}">
								<img src="{{ person.image }}" class="img-circle">
							</a>
							<div class="caption"><h3>{{ person.name }}</h3>
								<h5 class="email">{{ person.email }}</h5>
							</div>				
						</div>
					</div>
				{% if count >= 4 and forloop.last == false %} <!-- If we've reached a multiple of 4, end this row and start a new one, unless this is the last item -->
					</div>
					<div class="row row-centered">
						{% assign count = 0 %} <!-- Rest the count -->
				{% elsif forloop.last %}
					</div> <!-- End the row if this is the last item -->
				{% endif %}
			{% endfor %}
		{% endif %}
	{% endfor %}
</div>

<center><img src="../img/paragraph-separator-2.gif" width=10%></center>

<!--List of former group members -->
<h2 class="category-title">Former Group Members</h2>
<div class="container">
	{% for category in site.data.group_members %} <!-- Displays students with 4 students to a row, adding new rows if needed-->
		{% if category.name == "Former Group Members" %}
			{% assign count = 0 %} <!-- Initialize variable that counts to 4 -->
			{% for person in category.people %}
				{% assign count = count | plus: 1 %} <!-- Increment -->
				{% if forloop.first %} <!-- If this is the first item, start a row -->
					<div class="row row-centered">
				{% endif %}
					<div class="col-md-3 col-centered">
						<div class="thumbnail">
							
								<img src="{{ person.image }}" class="img-small-circle">
							 <div class="caption"><h3>{{ person.name }}</h3>
								<h5 class="email">{{ person.email }}</h5>
							</div>				
						</div>
					</div>
				{% if count >= 5 and forloop.last == false %} <!-- If we've reached a multiple of 4, end this row and start a new one, unless this is the last item -->
					</div>
					<div class="row row-centered">
						{% assign count = 0 %} <!-- Rest the count -->
				{% elsif forloop.last %}
					</div> <!-- End the row if this is the last item -->
				{% endif %}
			{% endfor %}
		{% endif %}
	{% endfor %}
</div>
